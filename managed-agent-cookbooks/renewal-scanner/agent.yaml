name: renewal-scanner
model: claude-sonnet-4-7
system:
  file: ../../renewals/agents/renewal-scanner.md
tools:
  - type: agent_toolset_20260401
    default_config: { enabled: false }
    configs:
      - { name: read,  enabled: true }
      - { name: write, enabled: true }
      - { name: task,  enabled: true }
  - { type: mcp_toolset, mcp_server_name: crm,         default_config: { enabled: true } }
  - { type: mcp_toolset, mcp_server_name: cs-platform,  default_config: { enabled: true } }
  - { type: mcp_toolset, mcp_server_name: usage,        default_config: { enabled: true } }
  - { type: mcp_toolset, mcp_server_name: support,      default_config: { enabled: true } }
  - { type: mcp_toolset, mcp_server_name: slack,        default_config: { enabled: true } }
mcp_servers:
  - { type: url, name: crm,         url: "${CRM_MCP_URL}" }
  - { type: url, name: cs-platform, url: "${CS_PLATFORM_MCP_URL}" }
  - { type: url, name: usage,       url: "${USAGE_MCP_URL}" }
  - { type: url, name: support,     url: "${SUPPORT_MCP_URL}" }
  - { type: url, name: slack,       url: "${SLACK_MCP_URL}" }
skills:
  - { from_plugin: ../../renewals }
callable_agents:
  - { manifest: ./subagents/pipeline-puller.yaml }
  - { manifest: ./subagents/risk-expansion-classifier.yaml }
  - { manifest: ./subagents/renewal-brief-composer.yaml }
